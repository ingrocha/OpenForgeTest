export interface GitUser {
  name: string;
  login: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  avatar_url: string;
  // eslint-disable-next-line @typescript-eslint/naming-convention
  repos_url: string;
  location: string;
  bio: string;
  company: string;
  blog: string;
}
